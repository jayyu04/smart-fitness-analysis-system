<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ 必要權限 -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>



    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Collegeproject"
        tools:targetApi="30">

        <!-- ✅ 首頁 (HomeActivity) -->
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ✅ 註冊其他 Activity，全部標上 android:exported="true" -->
        <activity
            android:name=".SettingActivity"
            android:exported="true"/>

        <activity
            android:name=".MovementSelectActivity"
            android:exported="true"/>

        <activity
            android:name=".FatigueSettingActivity"
            android:exported="true"/>

        <activity
            android:name=".HistoryActivity"
            android:exported="true"/>

        <!-- ✅ 新增 CameraFirstActivity -->
        <activity
            android:name=".CameraFirstActivity"
            android:exported="true"/>

        <activity android:name=".BluetoothActivity"
            android:exported="true" />

        <activity android:name=".DeviceTestActivity"
            android:exported="true" />

        <activity android:name=".InstructionActivity" />

        <activity android:name=".ParticipantActivity" />

        <activity android:name=".FatigueEventActivity" />

        <activity android:name=".RmsChartActivity"/>

        <activity android:name=".CloudPendingActivity"/>

        <service
            android:name=".ScreenRecordService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection"/>

        <service
            android:name=".FatigueService"
            android:foregroundServiceType="connectedDevice|dataSync"
            android:exported="false" />

    </application>

</manifest>
